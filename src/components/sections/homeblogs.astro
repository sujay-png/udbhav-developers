<!-- ---
import { getAllPosts } from "../../lib/wordpress";


const allPosts = await getAllPosts();
const latestPosts = allPosts.slice(0, 3);
---

<section class="home-blogs">
  <div class="container">
    <div class="section-heading">
      <span class="eyebrow">The Serenity Feed</span>
      <h2>Blog Articles</h2>
      <div class="ornament">
        <span></span>
      </div>
    </div>

    <div class="blogs-grid">
      {latestPosts.map((post) => <PostCard post={post} />)}
    </div>

    <div class="view-all">
      <a href="/blogs" class="btn-secondary">View All Articles</a>
    </div>
  </div>
</section>

<style>
  .home-blogs {
    padding-block: var(--space-xl);
    background-color: var(--color-bg);
  }

  .section-heading {
    margin-bottom: var(--space-xl);
  }

  .blogs-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-lg) var(--space-md);
  }

  .view-all {
    display: flex;
    justify-content: center;
    margin-top: var(--space-xl);
  }

  @media (max-width: 960px) {
    .blogs-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    .blogs-grid :global(article:nth-child(3)) {
      display: none; /* Hide 3rd on tablet to maintain grid symmetry */
    }
  }

  @media (max-width: 620px) {
    .blogs-grid {
      grid-template-columns: 1fr;
    }
    .blogs-grid :global(article:nth-child(3)) {
      display: block; /* Show again on mobile */
    }
  }
</style> -->
